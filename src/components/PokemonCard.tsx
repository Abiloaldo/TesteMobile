
type Demonstracoes = {
    //declarar função
    function: () => void,
    functionParams: (n1: string, n2: number) =>,    
    //parametro com dois possíveis tipos
    tipo: string | number,
    
}

type PokemonCard = {
    //colocar ponto de interrogação para identificar quando um parâmetro não é obrigatório
    nome: string,
    categoria: string,
    descricao: string,
}

const PokemonCard = () => {
    return (
        
    );
}

export default PokemonCard;